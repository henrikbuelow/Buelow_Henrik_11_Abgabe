package model;

import java.util.HashSet;

import main.VLSucheMain;

public class VLSucheController {
/*
	public static void search(String prof, String semester) {
		VLSucheMain.suche.clear();
		
		for (Vorlesung v : VLSucheMain.suche) {
			if (v.getProf() == prof && v.getSemester() == semester) {
				VLSucheMain.suche.add(v);
			}
		}
		
	}*/

}
